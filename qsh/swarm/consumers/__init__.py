"""qsh.swarm.consumers — Inbound prior consumption helpers.

INSTRUCTION-263B V5. Houses the in-memory prior cache. Consumer-side
enforcement (FlowController / CascadeController prior consumption,
ctx.swarm_priors enrichment) is NN-C scope.
"""
